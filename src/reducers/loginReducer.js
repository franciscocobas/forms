const INITIAL_STATE = {
  username: '',
  password: '',
  userToken: '',
  user: {}
};

export default (state = INITIAL_STATE, action) => {
  switch(action.type) {
    default: 
      return state;
  }
};